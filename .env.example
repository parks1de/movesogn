# Brevo — Transactional email + contact saving
# Get your key at: app.brevo.com → Settings → API Keys
BREVO_SECRET_KEY=your-brevo-api-key

# Sanity — content (project o76s1gxs already configured in src/lib/sanity.ts)
# No env var needed for public read access

# Google Analytics 4 (optional — leave empty to disable)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Site URL (used for sitemap + OG tags)
NEXT_PUBLIC_SITE_URL=https://www.movesogn.no
